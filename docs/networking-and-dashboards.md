# Networking and dashboards

## Glance
Glance is the lightweight dashboard used to keep the most important services visible at a glance.

## Nginx Proxy Manager
Nginx Proxy Manager is used for local reverse proxying and access management.

## Pi-hole
Pi-hole is hosted on a separate machine and is used for DNS filtering and ad blocking across the home network.
